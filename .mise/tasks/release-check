#!/usr/bin/env bash
#MISE description="Validate an ontology repo through refresh and bootstrap checks without committing or tagging"
#USAGE arg "<repo>" help="Path to the ontology repository to validate"
set -euo pipefail

exec node src/cli/ontology-release.mjs --repo "${usage_repo?}" --validate-only
