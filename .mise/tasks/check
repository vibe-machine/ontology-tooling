#!/usr/bin/env bash
#MISE description="Smoke-check the ont CLI by executing its --help paths"
set -euo pipefail

cargo run --quiet -p ont -- --help >/dev/null
cargo run --quiet -p ont -- validate-package --help >/dev/null
cargo run --quiet -p ont -- release --help >/dev/null
echo "check ok"
